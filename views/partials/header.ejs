<html>

<head>
  <title>Express static server</title>
  <script src='/public/vendor/jquery-3_3_1_min_js.js'></script>
  <link rel="stylesheet" href="/public/vendor/bootstrap.min.css" type="text/css">
  <link rel="stylesheet" href="/public/stylesheets/style.css" type="text/css">

</head>

<body>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="https://www.thehackingschool.com/">The hacking School</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav"
      aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item active">
          <a class="nav-link" href="/home">Home </a>
        </li>
        <li> <a class="new nav-link active" href="newTest">Add New Student Profile</a></li>
      </ul>

      <!-- <a href="/signup"><button class="btn btn-primary" id="sign">Signup</button></a>
      <a href="/login"><button class="btn btn-primary" id="log">Login</button></a>
      <a href="/users/logout"> <button class="btn btn-primary" id="logout">Logout</button></a> -->
<ul class="nav navbar-nav">
  <li class="<%= typeof page !== 'undefined' && page === 'test' ? 'active' : '' %>"></li>
</ul>
<ul class="nav navbar-nav navbar-right">
  <% if(!currentUser){ %>
  <li class="<%= typeof page !== 'undefined' && page === 'login' ? 'active' : '' %>"><a href="/login"> <button class="btn"> Login</button></a></li>
  <li class="<%= typeof page !== 'undefined' && page === 'register' ? 'active' : '' %>"><a href="/signup"> <button class="btn"> Sign Up</button></a></li>
  <% } else { %>
  <li><a href="#">Signed In As
      <%= currentUser.username %></a></li>
  <li><a href="/users/logout">Logout</a></li>
  <% } %>
</ul>


    </div>
  </nav>

    <div class="container">
      <% if(error && error.length > 0) { %>
      <div class="alert alert-danger">
        <p>
          <%=error %>
        </p>
      </div>
      <% } %>
    
      <% if(success && success.length > 0) { %>
      <div class="alert alert-success">
        <p>
          <%= success %>
        </p>
      </div>
      <% } %>
    </div>
